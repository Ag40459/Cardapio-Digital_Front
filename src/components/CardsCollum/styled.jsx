import { styled, Button } from '@mui/material';

export const CustomButton = styled(Button)({
    height: '1.5rem',
    display: 'flex',
    paddingTop: '1rem',
    width: '50%',
    backgroundColor: 'rgb(187, 35, 187)',
})